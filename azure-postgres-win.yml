steps:
- powershell: |
    iex (new-object net.webclient).downloadstring('https://get.scoop.sh')
    scoop update
    scoop install postgresql
  displayName: Install postgresql
- powershell: pg_ctl start
  displayName: Start postgresql
